#include "blockChain.h"
#include "hash.h"

int main(void){
  Blockchain G;
  menu(&G);
  // char c[] = "aaa";
  // char* tab = (char*)malloc(10*sizeof(char));
  // int j;
  // for(j = 0; j < 2; j++){
  //     *(tab+j) = *(c+j);
  //     printf("%c : %c\n", *(tab+j), *(c+j));
  // }
  // *(tab+j) = '\0';
  // printf("%d\n%s\n%s\n\n", strlen(c), c, tab);

  return 0;
}
